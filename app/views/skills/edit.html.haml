= simple_form_for(@skill, :html => {:class => 'form-vertical' }) do |f|
  = f.error_notification
  = display_base_errors @skill
  = f.input :name, :autofocus => true, :required=> true
  = f.button :submit, 'Save', :class => 'btn-primary'